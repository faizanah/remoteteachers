<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h3>Welcome to the <%= @site.name %>, <%= @user.name %></h3>
    <p>
      <% if @user.invited_by.present? %>
        You are invited by <%= @user.invited_by.name %> on <%= @site.name %>.</p>
        <% else %>
          You are invited on <%= @site.name %>.</p>
    <% end %>
    <p>
      To setup your account, just follow this link: <%= link_to 'click here', @url %>.
    </p>
    <p>Thanks!</p>
  </body>
</html>
